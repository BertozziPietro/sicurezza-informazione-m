services:
  level1:
    build: ./level1/server
    ports:
      - "2001:2001"
    environment:
      - FLAG=CTF{placeholder-flag-level1}
    restart: unless-stopped

  level2:
    build: ./level2/server
    ports:
      - "2002:2002"
    environment:
      - FLAG=CTF{placeholder-flag-level2}
    restart: unless-stopped

  level3:
    build: ./level3/server
    ports:
      - "2003:2003"
    environment:
      - FLAG=CTF{placeholder-flag-level3}
    restart: unless-stopped

  level4:
    build: ./level4/server
    ports:
      - "2004:2004"
    environment:
      - FLAG=CTF{placeholder-flag-level4}
    restart: unless-stopped

  level5:
    build: ./level5/server
    ports:
      - "2005:2005"
    environment:
      - FLAG=CTF{placeholder-flag-level5}
    restart: unless-stopped
